[build]
buildCommand = "npm ci"

[deploy]
startCommand = "node --import ./src/instrument.js src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
